import React from "react";

const Title = ({ text }) => {
  return (
    <div className="flex justify-center text-winc-black font-bold text-5xl font-urbanist">
      {text}
    </div>
  );
};

export default Title;
